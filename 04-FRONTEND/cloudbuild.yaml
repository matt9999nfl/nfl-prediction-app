# DEPRECATED — DO NOT USE
#
# This Cloud Build configuration is no longer the canonical frontend build pipeline.
# GitHub Actions (.github/workflows/frontend-deploy.yml) is the sole authorised
# deploy path for the frontend.
#
# This file is kept for historical reference only.  It MUST NOT be executed:
#   - It hard-codes VITE_API_KEY= (empty string), which breaks authentication.
#   - It does not read the API key from Secret Manager.
#   - No Cloud Build trigger points to this file (verified 2026-05-24).
#
# To trigger a frontend deploy: push a commit touching 04-FRONTEND/ to main, or
# run the GitHub Actions workflow manually from the Actions tab.
#
# If you need to trigger a manual deploy outside of GitHub Actions, follow the
# steps in 05-DEVOPS/RUNBOOKS.md or contact the DevOps lead.
