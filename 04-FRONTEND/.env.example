# Copy to .env.local and fill in values.
# In dev, the Vite proxy handles /api and /health so VITE_API_BASE_URL can be empty.
# In production, set it to the full backend URL (e.g. https://api.yourdomain.com).

VITE_API_BASE_URL=
VITE_API_KEY=your-api-key-here
